[Unit]
Description=USRP Remote Link (%i)
Documentation=https://github.com/rosenpoon/usrp-remote-link
After=network-online.target
Wants=network-online.target
StartLimitIntervalSec=60
StartLimitBurst=5

[Service]
Type=notify
ExecStart=/usr/local/bin/usrp-remote-link -c /etc/usrp-remote-link/%i.toml
Restart=on-failure
RestartSec=5
TimeoutStartSec=10
TimeoutStopSec=5

# Watchdog: daemon must send WATCHDOG=1 within this interval
WatchdogSec=5

# Security hardening
NoNewPrivileges=yes
ProtectSystem=strict
ProtectHome=yes
PrivateTmp=yes
# Allow access to ALSA and HID devices
DeviceAllow=/dev/snd rw
DeviceAllow=char-hidraw rw
SupplementaryGroups=audio

[Install]
WantedBy=multi-user.target
